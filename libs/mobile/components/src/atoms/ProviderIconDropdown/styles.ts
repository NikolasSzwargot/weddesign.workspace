import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    dropdown: {
        padding: 5,
    },
    iconContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 5,
    },
});
